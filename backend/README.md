# backend

See the repository [README](../README.md) for the full documentation.

```bash
python main.py            # bot + API
python main.py --check    # self-test
python main.py --once     # single screening cycle
python backtest.py --days 60
python -m pytest
```
